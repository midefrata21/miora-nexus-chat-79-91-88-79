
import React from 'react';
import { RealAutoCodeGenerator } from './RealAutoCodeGenerator';

const AutoCodeCore: React.FC = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50 dark:from-slate-900 dark:to-blue-900">
      <RealAutoCodeGenerator />
    </div>
  );
};

export default AutoCodeCore;
